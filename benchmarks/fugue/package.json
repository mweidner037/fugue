{
  "name": "fugue",
  "version": "1.0.0",
  "description": "Fugue list CRDT benchmarks",
  "main": "./run.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "start": "([ -f \"./dist/bundle.js\" ] || npm run dist) && node ../../bin/measure-bundle.js && node --expose-gc run.js && npm run table",
    "table": "echo 'Fugue results: \n\n' && node ../../bin/render-table.js ../results.json 6000 fugue",
    "dist": "npm run clean && rollup -c && gzip --keep dist/*"
  },
  "author": "TODO",
  "license": "MIT",
  "dependencies": {
    "@collabs/collabs": "0.6.1"
  }
}
